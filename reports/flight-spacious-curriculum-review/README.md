# Explicit spacious locomotion curriculum

A finite spacious scene is feasible without changing the fly, neural graph, motor transfer or flight-quality gates. The staged profile uses radius50cm and nominal ceiling50cm, retains the original fruit and odor field, and extends the existing bowl with a flat outer apron. This tests maintained locomotion separately from avoiding the original small boundary. It does not demonstrate success in the original habitat.

Saved telemetry motivates the separation. The original common.90/frequency227Hz run first reports environment contact at1.838s with up=.999753 and COM radius6.19982cm. The .80/frequency235.8Hz run first reports contact at1.274s with up=.964692 and radius6.21944cm; its first45° tilt occurs later. Their original v1 best qualified bouts were.602s and.172s, respectively. These posture samples do not imply uninterrupted qualification: the .90 trace already contains substantial downward velocity before contact, and the .80 instantaneous vertical-speed signal repeatedly crosses the current threshold. `evidence.json` contains exact timestamps/ranges and input hashes; `audit.mjs` reproduces them without simulation. Aggregate observations alone do not identify the individual contacting geom.

The implemented source stage is [`../flight-spacious-curriculum-staging`](../flight-spacious-curriculum-staging/README.md). Config `maintainedScene` and metadata `maintained_scene` must both contain exactly `{schemaVersion:1, profile:'spacious-maintained-flight-v1', radiusCm:50, ceilingCm:50, floorCapRadiusCm:6.5}`. Missing on both sides preserves the default; malformed or mismatched profiles fail. The configuration must contain only the maintained-flight stage. Root corrected inherited multi-stage configurations in separate config-only plans after the first launch rejected before physics; old plans/results remain preserved.

Native units are centimetres, grams and seconds; renderer distance is ten times native distance. The central floor is `z=.15+.037*r²` cm. Its analytic surface and original257×257 samples within radius6.5cm are retained. Beyond that radius the floor is capped at1.71325cm; retaining the uncapped parabola in a50cm room would create a tall surrounding bowl. The original square heightfield extends to±6.6cm. Four static slabs meet its capped outer edge and extend to±51cm. At the circular cap, mesh interpolation changes within a grid cell that straddles the kink; central-vertex parity is not a guarantee of continuous-surface parity throughout that band.

The64 wall segments have centre radius49.9cm, inner face49.85cm, and span z−1..50.15cm. The ceiling plane is at50.15cm, retaining the previous physical offset; the explicit score radius is50cm and nominal ceiling50cm with the unchanged.2cm allowance. All solids remain static world geoms. The original six fruit objects and189 fruit collision meshes remain intact. Fly body names, joints, masses, muscle model and dynamics are not changed.

Required source touchpoints are the habitat surface, native collision-scene builder, config/metadata agreement in the world adapter, explicit observation/scorer bounds, and preview bounds/cap. The strict native runner needs only an exact pinned-module resolution branch for newly staged canonical paths; all execution and provenance guards remain unchanged. Geometry metadata and source assets receive new hashes/fingerprints. This is an explicit task variant, not a global relaxation of failure bounds.

No new visual/height cue is added. Vision remains disabled in these plans. Fruit/odor values are unchanged everywhere; central contact feedback is retained at the sampled surface. Outer terrain and contact opportunities deliberately differ, and live neural feedback may diverge after trajectories differ. Haltere, tegula, motor and muscle paths are unchanged. Preview dimensions are display metadata rather than sensory input.

The .5s live held-root warmup, root release at3.5cm, five scored seconds, current continuous1s success requirement, attitude/rate/support/wing-power/contact tests and v1 instantaneous−1cm/s vertical-speed threshold remain unchanged in this frozen stage. Root's separately proposed50ms height-window v2 scorer is not included here.

Eleven pure source/geometry/scorer/resolution tests pass, including complete default collision parity and every staged module's syntax. A separate native validator is prepared but was not executed by this agent: it compares two sequential static models, uses no time steps, checks body mass/dimensions/inertia/joints/actuators/reference geometry and native wall/apron/heightfield-edge arrays. Dynamic cap/seam contacts, a same-input pre-contact trajectory comparison and longer flight behavior still require root's serial native evaluation.

```sh
node reports/flight-spacious-curriculum-review/audit.mjs
```
