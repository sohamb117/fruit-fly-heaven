The opt-in tegula pathway is connected and changes motor activity, but this paired seed shows no improvement in flight duration. Both episodes take off at 84 ms, attain 170 ms of continuous qualified flight, and terminate for excessive rotation at 398 ms. Neither maintains flight or lands.

The canonical offline report is [comparison-v2/README.md](comparison-v2/README.md), with complete per-cell/event-window statistics and source hashes in [comparison-v2/comparison.json](comparison-v2/comparison.json). It was generated by [compare.mjs](compare.mjs); the analyzer ran no simulation. The first comparison is preserved because it initially treated the added calibration-reference metadata as a selectivity mismatch. Version 2 verifies both reference hashes and the prior-to-reference link; raw inputs and numerical results are unchanged.

- Disabled-option regression: all nine gates reproduce the preceding DLM-ionic actual run, including every captured motor packet and the physical digest of initialization plus every completed 2 ms body block. Native integration steps between those samples are outside the digest's scope.
- Paired settings: same seed, 27 interpreter parameters, initial condition, native XML, runtime sources, Dawn Metal backend, DLM profile, event-muscle priors and objective. Only the declared tegula configuration and its provenance differ in the model metadata.
- Causal input timing: all 199 consecutive 2 ms inputs are reconstructed from the preceding row's measured load, with maximum independent numerical error 1.42e-14 Hz. An erroneous same-row reconstruction disagrees on all 398 side samples, by up to 33.21 Hz. Initial input is null. The cached native load is 50 microseconds older than each post-step body sample.
- Requested afferent rates: left mean 46.75 Hz, range 0.00114–77.56; right mean 46.64 Hz, range 0.00100–77.77. These are requested input rates, not measured afferent spikes. The input is an unsigned aerodynamic hinge-moment proxy with a frozen recruitment prior, not an anatomically established strain-axis or phase code.

Motor packets first differ at 72 ms; cumulative event counts first differ at 76 ms. B1 remains silent on both sides in both the full 398 ms episode and the fixed (100,280] ms window. Other steering channels respond: in that fixed window, B2 counts rise left 28→33 and right 26→30; I1 rises left 19→26 and right 43→46; I2 rises left 44→48 and right 45→50. These changes demonstrate that the feedback perturbs the existing neural circuit, not that it recruits the correct flight reflex.

DLM raw mean firing rates in the same (100,280] ms window fall left 37.78→27.78 Hz and right 41.11→32.22 Hz. DVM also falls, left 35.71→27.78 and right 40.48→28.57 Hz per motor neuron. Combined final wing power nevertheless remains approximately 1 in both arms. Actual excitation and force are available only at sparse preview times; those samples do not support matched-window force averages.

The old scorer gives baseline −1.119 and feedback −1.354 despite equal best earned progress (2.51), equal flight time and equal failure time. The difference comes from its terminal failure penalty: 3.629 versus 3.864 as sampled final angular speed rises 377.39→518.64 rad/s. This is not evidence that feedback learned anything or changed the achieved task milestone. Future reward changes are outside these frozen results.

Active execution takes 6.883 versus 6.914 wall seconds, or 17.293 versus 17.372 wall seconds per simulated second. The observed ratio is 1.0046; one pair is insufficient to isolate overhead or claim a performance improvement. No optimizer update occurred in either episode.
