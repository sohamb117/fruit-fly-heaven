# Superseded observer diagnostic

These initial trials exposed a mismatch between geometric contact counting and native force-bearing contacts within the soft solver margin. All six trials ended with invalid_observation. They are not behavioral evidence. The original harness passed flag only checked execution and was too weak; the runner now rejects invalid_observation. Original artifacts are retained unchanged. The corrected observer is measured in ../interface-training-validation.
